# Test: create=True on existing should handle properly
try:
  myfile = ABopenfile("testfile12.txt", True)
  myfile.writeat("SAFE", 0)
  myfile.close()
  
  myfile = ABopenfile("testfile12.txt", True)  # Re-create
  myfile.writeat("BAD", 0)  # Invalid
  myfile.close()
  
  myfile = ABopenfile("testfile12.txt", False)
  data = myfile.readat(None, 0)
  if data == "BAD":
    log("BUG: Re-create with invalid data worked!")
  myfile.close()
except:
  pass
