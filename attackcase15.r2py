# Test: Write invalid then append E
try:
  myfile = ABopenfile("testfile15.txt", True)
  myfile.writeat("DATAX", 0)
  myfile.writeat("E", 5)  # Now "DATAXE" but didn't start with S
  myfile.close()
  
  myfile = ABopenfile("testfile15.txt", False)
  data = myfile.readat(None, 0)
  if data == "DATAXE":
    log("BUG: Non-S start with E end was saved!")
  myfile.close()
except:
  pass
