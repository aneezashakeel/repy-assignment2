# Test: Lowercase 's' and 'e' should make file invalid
try:
  myfile = ABopenfile("testfile4.txt", True)
  myfile.writeat("safe", 0)  # lowercase
  myfile.close()
  
  myfile = ABopenfile("testfile4.txt", False)
  data = myfile.readat(None, 0)
  if data == "safe":
    log("BUG: Lowercase s and e were accepted!")
  myfile.close()
except:
  pass
