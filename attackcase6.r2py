# Test: Empty file should not create valid A file
try:
  myfile = ABopenfile("testfile6.txt", True)
  # Don't write anything
  myfile.close()
  
  myfile = ABopenfile("testfile6.txt", False)
  data = myfile.readat(None, 0)
  if len(data) > 0:
    log("BUG: Empty B created A file!")
  myfile.close()
except:
  pass
