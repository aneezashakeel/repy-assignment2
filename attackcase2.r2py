# Attack: Write data that doesn't start with 'S'
# Should not be saved to A file

if "testfile2.txt" in listfiles():
  removefile("testfile2.txt")
if "testfile2.txt.a" in listfiles():
  removefile("testfile2.txt.a")
if "testfile2.txt.b" in listfiles():
  removefile("testfile2.txt.b")

myfile = ABopenfile("testfile2.txt", True)
myfile.writeat("XVALIDX", 0)
myfile.close()
