# Test: File not starting with 'S' should be invalid
try:
  myfile = ABopenfile("testfile2.txt", True)
  myfile.writeat("DATAE", 0)  # Doesn't start with 'S'
  myfile.close()
  
  myfile = ABopenfile("testfile2.txt", False)
  data = myfile.readat(None, 0)
  if data == "DATAE":
    log("BUG: File not starting with S was saved!")
  myfile.close()
except:
  pass
