# Test: Overwrite middle preserving S and E
try:
  myfile = ABopenfile("testfile14.txt", True)
  myfile.writeat("SECURE", 0)
  myfile.close()
  
  myfile = ABopenfile("testfile14.txt", False)
  myfile.writeat("XX", 2)  # Middle corruption
  myfile.close()
  
  myfile = ABopenfile("testfile14.txt", False)
  data = myfile.readat(None, 0)
  if "XX" in data:
    log("BUG: Middle corruption still has S and E!")
  myfile.close()
except:
  pass
