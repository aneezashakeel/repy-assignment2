# Attack: Empty file should not save
if "testfile7.txt.a" in listfiles():
  removefile("testfile7.txt.a")
if "testfile7.txt.b" in listfiles():
  removefile("testfile7.txt.b")

myfile = ABopenfile("testfile7.txt", True)

try:
  # Don't write anything
  myfile.close()
  
  myfile = ABopenfile("testfile7.txt", False)
  data = myfile.readat(10, 0)
  if len(data) > 0:
    log("Empty B file created A file!")
  myfile.close()
except:
  pass
