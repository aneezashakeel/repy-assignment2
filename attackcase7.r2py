# Test: File not ending with 'E' should be invalid
try:
  myfile = ABopenfile("testfile7.txt", True)
  myfile.writeat("SDATA", 0)  # No 'E' at end
  myfile.close()
  
  myfile = ABopenfile("testfile7.txt", False)
  data = myfile.readat(None, 0)
  if data == "SDATA":
    log("BUG: File not ending with E was saved!")
  myfile.close()
except:
  pass
