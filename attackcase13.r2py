# Test: File not ending with E
try:
  myfile = ABopenfile("testfile13.txt", True)
  myfile.writeat("SAFEDATA", 0)
  myfile.close()
  
  myfile = ABopenfile("testfile13.txt", False)
  data = myfile.readat(None, 0)
  if data == "SAFEDATA":
    log("BUG: File ending with non-E was saved!")
  myfile.close()
except:
  pass
